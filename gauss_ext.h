#pragma once

#include <iostream>
#include <vector>
#include <cmath>

namespace N {
    class Gauss {
    public:
        std::vector<std::vector<double>> gauss(std::vector<std::vector<double>> &matrix);
    };
}
